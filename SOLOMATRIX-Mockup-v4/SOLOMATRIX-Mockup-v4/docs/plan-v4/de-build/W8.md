# ĐỀ W8 — POLISH `js/sm-onboard.js` THEO PHẢN BIỆN W0 (câu chữ + hành vi wizard)

Bạn sở hữu DUY NHẤT `solomatrix-v3-gialai/js/sm-onboard.js`. **CHỈ Edit từng chỗ — TUYỆT ĐỐI CẤM Write
đè cả file** (Claude đã vá 3 chỗ trong file sau lượt W1: datConMa/docConMa dùng khoá 'smv3:ui' + đọc cả
`obConMa` — Write đè là mất vá). File hiện ~1.800 dòng, ĐỌC bằng Grep + Read theo offset quanh chỗ sửa.

NGUỒN LỆNH: `docs/plan-v4/out-build/W0-PHAN-BIEN-CAU-HOI.md` — đọc TOÀN BỘ. Mọi verdict/lời viết lại
trong đó là quyết định ĐÃ ĐƯỢC DUYỆT, bạn thi hành (Quang 20/08: «luôn đặt mình là end user mù công nghệ»).

## Việc, theo thứ tự ưu tiên (đối chiếu W0 mục 2 + mục 3 + mục 1):

1. **[CHẮN — W0 câu 4(d)] Nhóm LUẬT không sinh từ câu đoán:** sửa `danhMucCho`: `doanhThuUoc='tren-1-ty'`
   KHÔNG còn tự sinh nhóm BẮT BUỘC; nhóm LUẬT chỉ sinh khi (i) `t.vuotLuc` có thật (số bán thật vượt —
   `D.mocVuotNguong`), hoặc (ii) hộ bấm nút khẳng định mới «Tôi chắc cả năm bán trên 1 tỷ» ở câu 4
   (lưu `traLoi.chacTren1Ty=true`). Mọi nhánh không khẳng định → mục «Canh ngưỡng» + dòng «App đang theo
   dõi doanh thu để biết khi nào cần hoá đơn». Câu 4 chỉ còn vai trò XẾP THỨ TỰ gợi ý.
2. **[W0 câu 0(c,d)] Câu 0 đáp án bằng TÊN người** lấy từ seed (`t.chuHo`, `t.keCan`, `t.nguoiLonTuoi`;
   tenant trắng không có thì giữ nhãn vai) + câu phụ 1 chạm «Cô/chú tự bấm được không, hay để tối
   {tên kế cận} bấm giúp?» — «để cháu» thì rút gọn bất kể vai; thêm nút «Đổi người bấm» ở ĐẦU mọi màn
   wizard còn lại (đặt cạnh nút «Để sau»).
3. **[W0 câu 1–5(c)] Thay câu chữ 5 câu nhận diện đúng NGUYÊN VĂN W0 đề xuất** («Nhà mình làm nghề gì?»
   với «Du lịch: đón khách, ăn uống, nghỉ trọ»; «Nhà mình có cái giấy phép bán hàng treo tường chưa?» +
   4 đáp án mới, «Không nhớ — để cán bộ xem giúp» là nút to mặc định; câu 3 thêm dòng đếm «Đã chọn N chỗ»
   + nút lớn «Hết rồi, tiếp tục» luôn sáng + «bấm lần nữa để bỏ»; câu 4 «…đại khái thôi?» + dòng phụ gấp
   còn 1 câu + nút «Xem vì sao app hỏi»; câu 5 «Hiện nay bán hàng nhà mình ghi ở đâu?» + «Có cái máy in
   bill ở quầy»). BỎ hành vi «bấm 2 đoạn hội thoại thật» ở câu 3 (W0 bác — hộ sợ app đọc tin nhắn).
4. **[W0 câu 1(d)] Nút «Sửa câu trước»** trên từng màn con OB-2 (bấm lùi 1 câu, giữ trả lời cũ làm mặc định).
5. **[W0 #3] Ô gõ mã suất**: prefix «GL26-» cố định hiển thị sẵn, hộ chỉ gõ 8 ký tự sau, nhận cả dán;
   [W0 #2] dòng cạnh máy quét: «Quét mã giấy của cán bộ — không mất tiền, không điền gì cả.»
6. **[W0 #8] Màn danh mục OB-3**: mỗi dòng mở đầu bằng VIỆC đời thường in đậm («Tiền về quầy tự hiện lên»),
   tên dịch vụ thành chữ phụ nhỏ bên dưới.
7. **[W0 #9–#11] Luồng connector**: SePay màn trung gian đóng khung «Đang sang trang của NGÂN HÀNG —
   giống đưa thẻ cho thu ngân» + nút «Quay lại app» luôn hiện; grep cả file chữ «API» và «bên thứ ba» —
   không được xuất hiện trong chuỗi hiển thị cho hộ (chỉ được nằm trong khối «cài đặt nâng cao» /comment);
   «Đang chờ {tên thật: Zalo/cổng thuế/ngân hàng} — dự kiến {ngày}».
8. **[W0 #12 + 4 điểm soi] Thẻ đỏ quá hạn đổi giọng trấn an**: «Cái này chậm hơn hẹn — KHÔNG phải lỗi của
   cô/chú. Bấm để cán bộ gọi lại.» (kèm tên cán bộ nếu có `coCanBo`); hộ cháy hạn 30 ngày: chữ «cần xử lý»
   → «cán bộ sẽ liên hệ hôm nay» khi có `coCanBo`.
9. **[W0 #13] Nhãn «chưa đo — radar đang hỏi Q-0xx»** trước mặt hộ chỉ còn «Chưa đo được»; mã Q-0xx chuyển
   vào chuỗi nguồn-màn-hình/khối nâng cao.
10. **[W0 #14] «Việc giao người nhà» → câu có tên**: «Phần còn lại để tối {tên kế cận} bấm — app đã để sẵn»;
    kèm nút «Nhắc {tên} qua Zalo» (mở sheet chữ soạn sẵn để hộ TỰ gửi bằng Zalo thường — không tự gửi, N-06/N-09).
11. **[W0 P13/#15] Màn Tạm dừng**: bước gõ «XOÁ» thay bằng **bấm-và-giữ nút 5 giây có thanh chạy** (giữ
    2 lần xác nhận trước đó).
12. **[W0 P4] Không lộ «800 triệu»/«ngưỡng»/«lũy kế»** trong chuỗi hiển thị: thẻ canh ngưỡng nói «Theo cách
    bán hiện nay, cuối năm nhà mình có thể phải dùng hoá đơn điện tử — app canh giùm, tới lúc cần sẽ báo
    trước 30 ngày.» Số 800tr chỉ trong nguồn màn hình/nâng cao.
13. **[W0 P12] Màn chụp giấy tờ** thêm câu thứ hai: «Ảnh cô chụp chỉ nằm trong máy của cô. Gửi đi chỉ là
    tin "đã có giấy", không gửi tấm ảnh.»
14. **[W0 câu 2(d)] Trạm**: thẻ «Đang chờ» thêm nút phụ «Thực ra nhà mình chưa có» (hạ trạng thái về
    `chua_co_tk` + sinh việc cán bộ); khối «Nối tiếp» thêm nút «Bây giờ nhà mình bán thêm chỗ khác»
    (gọi lại `danhMucCho`); [W0 C3] OB-1 thêm nút «Xem thử trước khi có mã» → chuyển sang tenant
    `cd4-moi` và chạy wizard thật ở đó (hộ demo, không đụng hộ thật).
15. **[W0 P8] Thẻ MISA nhóm ĐỂ SAU (CD1)** thêm câu: «Nhà mình đang dùng MISA — OPC không thay nó; phần
    nối sang nhau đang kiểm tra, xong sẽ báo.»

Ràng buộc: giữ nguyên chữ ký hàm INTERFACE mục 3 (không đổi tên export); mỗi Edit có neo; sau khi xong
grep chứng minh: 0 chuỗi hiển thị chứa «API»/«bên thứ ba»/«Q-0» ngoài khối nâng cao & nguồn màn hình;
«GIỮ 5 GIÂY» tồn tại; nút khẳng định 1 tỷ tồn tại; báo cáo `out-build/W8-BAO-CAO.md` + dòng cuối
`BUILD-AGENT-DONE W8 <số Edit>`.
