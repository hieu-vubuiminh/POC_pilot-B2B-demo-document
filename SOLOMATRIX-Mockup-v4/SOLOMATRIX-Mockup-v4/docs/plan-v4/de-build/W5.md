# ĐỀ W5 — VIẾT MỚI `js/sm-b2g.js` + Edit nhỏ `b2g.html` (Sổ trực onboarding)

Bạn sở hữu DUY NHẤT 2 file trên + báo cáo. Thi công theo INTERFACE mục 6 + PLAN-V4 B.11 + D-#12 + CHOT P3/P6.

ĐỌC TRƯỚC: toàn bộ `b2g.html` (21k — đọc hết): cấu trúc nav/section/style hiện có, cách nó đọc SM/D;
`js/sm-seed-b2g.js` sẽ có `SM.seedB2G()` trả 48 hộ (INTERFACE mục 5 — W4 làm song song, cứ gọi đúng tên,
có fallback «đang nạp dữ liệu» nếu hàm chưa có).

## `js/sm-b2g.js` (Write MỚI, namespace `SM.b2g`):
1. **Sổ trực onboarding** — 6 khối đúng bảng PLAN B.11: (1) Suất QR: bảng mã GL26 (sinh mới = nút «Sinh 10 suất»
   → lưu `smv3:b2g-suat`, trạng thái chưa cấp/đã in/đã dùng, ai in CB-xx, «In QR» = sheet hiện mã to + ô QR
   giả lập); (2) Hộ ai theo (map hộ↔cán bộ, lọc theo CB); (3) Xác nhận tay (đọc nhật ký các dòng có maCanBo);
   (4) Hạn 30 ngày (hộ vuotNguong: vàng <7 ngày, đỏ quá hạn); (5) Quá hạn chờ (cho_duyet vượt hanDuKien);
   (6) Việc hoãn nhóm LUẬT. Dữ liệu = `SM.seedB2G()` + 3 tenant thật (CD1 phải xuất hiện ở khối 4).
2. **«Việc hôm nay của cán bộ»**: gộp khối 4+5+6 thành danh sách việc theo CB đang chọn, mỗi việc 1 dòng
   hành động («Gọi lại Hộ số 12 — Zalo OA quá hạn 2 ngày») — bấm mở chi tiết hộ (trạng thái + ngày, KHÔNG
   doanh thu — ranh giới IV.8).
3. **Định mức nhân sự**: số hộ/cán bộ + biểu đồ cột phân bố phút-tới-việc-đầu (có cán bộ vs tự làm) từ
   `phutTuKichHoat` — **mọi cột annotate giá trị** (quy ước dashboard QNSC); chú thích «số mô phỏng demo».
4. **Nhóm đợt nộp OA**: bảng đợt (tuần) → số hồ sơ, ngày dự kiến duyệt (+2–3 ngày làm việc [Q-004]).
5. **Khối «Căn cứ hành vi»** (D-#12): 4 con số [Q-004] kèm đúng nhãn hạn chế «2021, khảo sát Facebook ủy quyền,
   n=999» — chữ nhãn hiện cạnh số, không giấu.

## `b2g.html` (Edit nhỏ — không phá khối cũ):
Thêm 2 thẻ script (`js/sm-seed-b2g.js?v=20260820`, `js/sm-b2g.js?v=20260820`) sau các script hiện có;
1 mục nav «Sổ trực»; 1 `<section id="sotruc">` container mà sm-b2g render vào; các thẻ js hiện có thêm `?v=20260820`.

Ràng buộc: style tái dùng class có sẵn của b2g.html; chữ cho cán bộ được phép «nghiệp vụ» hơn chữ cho hộ
nhưng vẫn tiếng Việt thường; không CJK; báo cáo dán grep: 6 khối tồn tại, nav có Sổ trực, không sửa section cũ.
