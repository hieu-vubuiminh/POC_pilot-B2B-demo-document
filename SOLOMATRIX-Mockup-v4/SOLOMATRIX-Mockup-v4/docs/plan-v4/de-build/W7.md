# ĐỀ W7 — EDIT 5 FILE NHỎ: `js/sm-ai.js` · `js/sm-ops.js` · `js/sm-program.js` · `index.html` · `web.html`

Bạn sở hữu DUY NHẤT 5 file trên (Edit từng chỗ) + báo cáo. Thi công theo INTERFACE mục 8 + PLAN-V4 D-#4/#7/#9 + C.15.

ĐỌC TRƯỚC: sm-ai.js vùng handler lớp A (grep `handlers` / các câu hỏi mẫu) + afterHours (grep `afterHours`/`SLA`);
sm-ops.js `soanNhacNo`/`guiNhacNo` (~302–357); sm-program.js cấu trúc P.* + chỗ index.html render giá
(grep `gia`/`IV.9` trong index.html); web.html chỗ đọc connector (grep `connector`/`D.connectors`).

## 5 việc:
1. **sm-ai.js — 3 A-handler mới** (D-#7): «đơn mới nhất về lúc nào, từ đâu» (đọc sổ sự kiện sm-inbox — nguồn,
   giờ, payload tóm tắt, kèm «tính từ đâu» như handler hiện có) · «kênh nào đang đứt / kết nối ổn không»
   (đọc `D.connectors(t)` trường `trangThai`) · «dữ liệu tươi tới đâu» (đọc `doTuoi` — dòng chưa đo trả
   «chưa đo — radar đang hỏi Q-0xx», nhãn quy ước `CHUA_DO`). Thêm hằng `CHUA_DO = 'chưa đo'` dùng chung.
2. **sm-ai.js — afterHoursReply**: bổ câu «Trợ lý soạn sẵn trong 1 phút, cô chú đọc rồi tự bấm gửi» (N-06).
3. **sm-ops.js — `soanNhacNo`** trả thêm trường `cuaSo` = `SM.onb ? SM.onb.cuaSoTin(t, tenKhach) : null`
   (fallback null an toàn); KHÔNG đổi chữ ký cũ (mobile W6 đọc trường mới này).
4. **sm-program.js + index.html — bảng giá vs KiotViet** (D-#9, [Q-007]): khối `DOI_THU` trong sm-program
   (3 gói 270k/330k/490k + phụ phí +270k/+375k/chi nhánh, +150k/kho + 3 dòng kèm 0đ: HĐĐT, chữ ký số, PM kế toán
   hộ KD + câu định vị: «KiotViet 270k mua phần mềm bán hàng kèm tuân thủ 0đ; bên mình bán việc-được-làm-xong
   đa kênh + trợ lý 24/7 + người xuống cơ sở; lớp tuân thủ bên mình cũng 0đ» + ghi chú «mốc giá đối chiếu:
   Q-039»); index.html render bảng này CẠNH bảng giá hiện có + thêm 1 mục hồ sơ «Độ tươi dữ liệu per-connector»
   (render từ `D.connectors` của tenant mẫu, đủ cột C.15).
5. **web.html**: mọi chỗ đọc id connector cũ đi qua `D.chuyenId` (fallback `|| id`); nếu web.html gọi
   `toggleConnector` thì thay bằng chữ trạng thái tĩnh «quản lý kết nối trên điện thoại của hộ» (web là màn
   kế toán chia sẻ, không nối hộ).

Ràng buộc: mỗi Edit có neo; số tiền KiotViet đúng từng con [Q-007] không thêm bớt; chữ hiển thị = lời thường;
báo cáo dán grep: 3 handler mới, DOI_THU tồn tại, index có 2 khối mới, web không còn toggleConnector.
