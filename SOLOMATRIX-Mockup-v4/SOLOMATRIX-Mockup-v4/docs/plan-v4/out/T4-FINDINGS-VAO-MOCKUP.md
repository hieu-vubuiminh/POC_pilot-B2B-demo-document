# T4 — FINDINGS RADAR → MOCKUP v3 CÒN THIẾU GÌ

- Vai: thợ mỏ đối chiếu (đề T4, chiến dịch PLAN v4).
- Ngày soát: 2026-08-20. Đối tượng: mockup v3 tại `solomatrix-v3-gialai/` (đây là bản GIA LAI, khác mockup v2 mà sổ REQUIREMENTS từng đối chiếu).
- Cách soát: mọi `file:dòng` dưới đây là kết quả Grep/Read thật trên v3 trong lượt này; mọi số khí giới kèm nhãn `[Q-00x]` / `[R-xx]` / `[N-xx]`; số ngoài bảng cho phép được trích từ finding tương ứng (đã Read cả 8 findings + REQUIREMENTS + ANTI-SCOPE + DIGEST 20/08).
- Tóm kết 1 đoạn: v3 đã mạnh ở lớp «nghiệp vụ trong tay» (ngưỡng 1 tỷ có hàm tính, thẻ nhắc có nút đi thẳng, nhắc nợ nối hội thoại, hàng đợi offline có retry) nhưng **toàn bộ lớp «nền tảng 24/7 nói thật về mình»** — độ tươi, agent ngủ/dedup, connector chết, lead-time cấp quyền, phí tin Zalo, thuế sàn nộp thay, giá so KiotViet — **chưa có màn nào**. Radar đã kiểm chứng xong đúng những chỗ này; dưới đây là 12 điểm chạm sản phẩm.

## 1. Bảng đối chiếu — mỗi dòng một «điểm bằng chứng chạm sản phẩm»

Khuôn: `#` · điểm bằng chứng · v3 đang có gì · còn thiếu gì · đề xuất · mức · effort · tiêu chí nghiệm thu.
Vị trí file viết tắt: `M:` = mobile.html, `I:` = index.html, `B:` = b2g.html, `W:` = web.html; `js/` giữ nguyên tiền tố.

| # | Điểm bằng chứng | v3 đang có gì (file:dòng thật) | Còn thiếu gì | Đề xuất (màn/thẻ/hàm) | Mức | Effort | Tiêu chí nghiệm thu |
|---|---|---|---|---|---|---|---|
| 1 | **Đồng hồ 30 ngày đăng ký HĐĐT khi vượt ngưỡng 1 tỷ** [Q-001: «đăng ký trong 30 ngày kể từ ngày cuối cùng của kỳ tính thuế có doanh thu lũy kế trên 01 tỷ đồng», NĐ 141/2026 sửa NĐ 68/2026] | `js/sm-domain.js:31-33` có `TAX.nguong` (posInvoice = 1e9); `js/sm-domain.js:157-169` `needsPosInvoice()` trả `yearRev/projected/thuocDien/sapVuot`; `js/sm-domain.js:240-245` thẻ `nguong-mtt` trong `deadlines()` — nhưng **chỉ hiện khi `sapVuot`**; `js/sm-seed-gialai.js:10` CD1 đã vượt 1 tỷ; `js/sm-ai.js:246-255` trả lời hỏi «1 tỷ» bằng số thật | Khi hộ **đã vượt** (CD1, `thuocDien=true`) không còn thẻ nào — đồng hồ 30 ngày không tồn tại; không có mốc «ngày cuối kỳ tính thuế vượt ngưỡng» trong kho để đếm; CD2 (780tr, ước cả năm 1.243tr — seed) là chỗ demo đắt nhất mà thẻ chỉ nói «trên đà vượt», không nói «vượt thì phải làm gì trong bao lâu» | (a) Hàm `moc30Ngay(t)` trong sm-domain: quét `revenueLines` theo quý luỹ kế từ 01/01, tìm quý đầu tiên vượt 1e9, mốc = ngày cuối quý đó [Q-001]; (b) thẻ `nguong-30n` trong `deadlines()` khi `thuocDien` hoặc đã có mốc: «Còn N ngày đăng ký hoá đơn điện tử — hết ngày D», `dichDen` theo sẵn map `DICH` (`js/sm-domain.js:260-261`); (c) với CD2: thẻ «đà vượt» thêm dòng «nếu đà này giữ nguyên, quý X sẽ vượt → chuông 30 ngày bắt đầu chạy cuối quý đó»; (d) màn đích của nút: xem giải thích KB-02 (`js/sm-ai.js:36-39`) + checklist chuẩn bị. Giao onboarding lần-đầu-xuyên-ngưỡng: **giao T1** | PHẢI | S | CD1 mở app thấy thẻ «còn N ngày đăng ký HĐĐT» với N tính từ seed (không viết cứng); CD2 thấy thẻ «đà vượt + quý dự kiến vượt»; hỏi trợ lý «bao giờ phải đăng ký hoá đơn» trả lời kèm «tính từ đâu» |
| 2 | **Bảng ngân sách độ tươi per-connector** [Q-006, R-A3-02 ❌] | `js/sm-inbox.js:23-31` NGUON định nghĩa 9 nguồn, cả 9 ghi `kenh:'webhook'` nhưng không khai độ tươi; `js/sm-domain.js:871` `dongBoCuoi` luôn = `SM.CLOCK.today` (không phải «X phút trước»); grep `độ tươi\|phút trước` trên *.html của v3 = **0 kết quả** (lượt soát này; DIGEST 20/08 grep trên v2 cũng 0) | Không màn nào công bố «dữ liệu này tươi bao nhiêu, lấy từ webhook hay polling»; mọi nguồn đều ghi webhook là nói quá (đơn sàn/tồn kho/vận đơn/tờ khai chưa đo — Q-036/Q-037 [Q-006]) | Màn mobile mới «Độ tươi dữ liệu» đặt trong `ketnoi` (chung trạm với T1 — **phần nhúng vào onboarding giao T1**, phần bảng là của mục này): mỗi connector 1 dòng — độ tươi công bố (tiền về = giây–phút [Q-006]; HĐĐT = phản hồi 15 phút–1 ngày làm việc [Q-002]; chưa đo thì ghi nhãn «chưa đo — radar đang hỏi Q-036/Q-037»), webhook-hay-polling, «cập nhật lần cuối X phút trước» tính từ `dongBoCuoi` (đổi `js/sm-domain.js:871` sang mốc giờ từ seed + hàm fmt). Nội dung chuẩn bảng lấy đúng đặc tả T2 (`out/T2-CONNECTOR-SPEC.md:264-281` — phối hợp T2, không đặc tả lại ở đây); thêm 1 mục hồ sơ ở I: | PHẢI | M | Màn ketnoi hiện đủ số connector đang nối; mỗi dòng có con số phút «lần cuối» tính từ hàm; dòng chưa đo hiện nhãn «chưa đo» thay vì trống (khớp R-A3-04); không dòng nào viết «realtime» cho nguồn chưa có bằng chứng |
| 3 | **Màn «agent đang ngủ / đánh thức bởi gì» + dedup** [Q-003: durable execution — «pauses execution without consuming compute», wait không tốn phí; webhook «events will be missed. Not might. Will.»; poll an toàn tối thiểu 24h; idempotency store trước connector đầu; R-A3-06/07 ❌] | `js/sm-ai.js:321` ghiChu «Trợ lý trực 24/7» (lời khẳng định, không cơ chế); `js/sm-core.js:169-182` `enqueue` + `js/sm-core.js:186-215` `drain` có retry `tries<3` + failRate — đây là hàng đợi **chiều ra**; `js/sm-inbox.js:189-288` KICH_BAN 9 kịch bản giả lập webhook chiều vào | Không khái niệm trạng thái nghỉ/đánh thức (timer, webhook, poll 24h); **không chống trùng**: `js/sm-inbox.js:187` `seq()` sinh mã tăng dần nên bấm kịch bản 2 lần tạo 2 giao dịch khác nhau — at-least-once chưa được mô phỏng, trong khi đó là trường hợp THIẾT KẾ chứ không phải lỗi [Q-006] | (a) Thẻ/màn «Trợ lý chạy nền» trong `them` hoặc `tainguyen`: bảng 3 cột — *việc đang chờ* (timer đếm mốc 30 ngày dòng 1, đếm hạn kê khai), *đánh thức bởi gì* (webhook từ nguồn N / poll đối soát 24h [Q-003] / hộ mở app), *khi ngủ tốn gì* («không tốn tài nguyên — trạng thái nằm ở bộ nhớ bền» [Q-003]); (b) Kịch bản giả lập mới trong KICH_BAN: «SePay gửi TRÙNG» — bấm 2 lần cùng một `id`, ghi nhận hộp thư: bản 2 hiện trạng thái «bản trùng theo id — đã bỏ, không cộng tiền lần hai», nhật ký thao tác ghi «chặn N bản trùng»; (c) cột «nguồn đánh thức» hiện đúng cho mọi event trong hopthu. Mô phỏng trung thực: dedup bằng chính kho localStorage, không giả vờ có backend | PHẢI | M | Bấm kịch bản «tiền về» 2 lần liên tiếp: tiền chỉ cộng 1 lần, có dòng nhật ký «đã chặn 1 bản trùng cùng id» hiển thị cho người chấm; màn «Trợ lý chạy nền» liệt kê được ≥3 việc đang chờ + nguồn đánh thức của từng việc |
| 4 | **Chính sách tin Zalo trong Hội thoại + hàng đợi gửi đi** [Q-005: cửa sổ tương tác 7 ngày; 8 tin miễn phí/48h rồi 55đ/tin; tin Giao dịch 165đ/tin; broadcast chỉ 4 tin/tháng gói Premium → N-09] | `M:2326-2353` viewHoiThoai (đếm chưa trả lời/đã gửi, tag `nhắc nợ` ở `M:2343`); `M:2226-2249` sheetNhacNo «Gửi qua Zalo»; `js/sm-ops.js:302-357` `soanNhacNo/guiNhacNo` — tin đi `kenh:'zalo'`, `enqueue('channel')`, có `tinhTu`; `js/sm-inbox.js:261-266` kịch bản khách nhắn 22:14 | **Không biết gì về 7 ngày**: nút nhắc nợ/upsell gửi như kênh không ma sát — khách im lặng >7 ngày thì tin Tư vấn thật sẽ không tới; không hiện quota 8 tin/48h; không hiện chi phí 55đ/165đ; không có đường thay thế ZNS (trả phí + template duyệt) khi hết cửa sổ | (a) Hàm `cuaSoTin(t, khach)` tính từ `t.messages` (tin cuối 2 chiều): trả «còn trong cửa sổ 7 ngày» / «im lặng X ngày — hết cửa sổ» + số tin miễn phí còn trong 48h + giá đường gửi được [Q-005]; (b) sheetNhacNo hiện dòng chính sách + chi phí **trước** nút gửi: trong cửa sổ = «tin Tư vấn, miễn phí (còn k/8)»; hết cửa sổ = «phải dùng tin Giao dịch 165đ/tin hoặc ZNS template đã duyệt» + tổng tiền nếu gửi cho N khách; (c) hàng đợi gửi đi (queue `channel`) hiện trạng thái từng tin; (d) seed thêm 1 khách tương tác cuối >7 ngày để demo. Không dùng broadcast làm kênh vận hành (N-09); người bấm gửi vẫn là hộ (khớp N-06 tinh thần «NGƯỜI bấm») | PHẢI | M | Với khách im lặng >7 ngày: sheet nhắc nợ hiện «hết cửa sổ — 165đ/tin» và nút đổi sang «soạn ZNS»; trong cửa sổ: hiện «miễn phí, còn k/8 tin trong 48h»; mọi con số nhãn [Q-005]; gửi xong hàng đợi hiển thị tin đang chờ |
| 5 | **Connector chết phải báo ra tiếng** [R-A3-05; Q-003: webhook best-effort — chết là SẼ XẢY RA] | `js/sm-domain.js:861-875` connectors(): `noi/tuNgay/dongBoCuoi/donVe` — không có trạng thái lỗi; `js/sm-domain.js:877-886` toggleConnector chỉ nối/ngắt; `M:866-895` viewKetnoi: nút «Nối/Đã nối» mãi xanh; lớp sự kiện đơn lẻ có lỗi (`js/sm-inbox.js:175` `trangThai='loi'`; `js/sm-domain.js:224-229` thẻ hoá đơn chưa truyền; `M:288` nhãn «lỗi, sẽ thử lại»; `js/sm-core.js:201-203` job lỗi có note) | Không có khái niệm «kết nối đứt»: connector chết thì mọi thứ im lặng đúng điều R-A3-05 cấm; không kịch bản giả lập cho đứt kết nối; không lời thoại chủ hộ hiểu được | (a) Trường `trangThai` per connector trong `t.connections` (song: `ok\|chua-noi\|chet`); (b) kịch bản giả lập «kết nối đứt» (dùng sẵn failRate của `js/sm-core.js:244` hoặc cờ trong seed): webhook nguồn sàn hết đến >24h → poll đối soát phát hiện [Q-003]; (c) thẻ trong `deadlines()`: «Kênh Shopee không đẩy đơn 2 ngày — có thể kết nối đứt» + `dichDen:'ketnoi'`; (d) màn ketnoi: tag đỏ «đứt — kiểm tra» thay nút «Đã nối», kèm lời thoại: «Hộ không cần hiểu kỹ thuật: bấm Kiểm tra, hệ thống tự thử lại và báo kết quả; nếu vẫn chết thì gọi cán bộ theo SLA» (nối `js/sm-ai.js:313-319`) | PHẢI | S | Bấm kịch bản «đứt»: thẻ cảnh báo xuất hiện trong «Việc cần để ý» (M:232-243), màn ketnoi hiện tag đỏ, nút «Kiểm tra» chạy thử lại rồi báo «đã sống lại» hoặc «cần cán bộ — SLA 15 phút» (mức SLA khớp `js/sm-ai.js:316`) |
| 6 | **Trạng thái kết nối + lead-time cấp quyền từng dịch vụ** [Q-002: SePay self-serve ~phút, 12+ ngân hàng; cổng CQT 15 phút–1 ngày làm việc, NCC Viettel/VNPT/MISA/FPT/SePay; Shopee cần app publish + shop ủy quyền, token 4h/refresh 1 tháng, hạn siết 27/05/2026; MISA qua NVKD; Q-004: Zalo OA cần GP ĐKKD, duyệt 2–3 ngày làm việc] | `M:866-895` viewKetnoi: nhóm dịch vụ + «danh mục tối thiểu» (`js/sm-domain.js:889-908` có `choVanHanh` cho nền tảng Nhà nước chưa vận hành), «Nối từ ngày + X đơn về»; NGUON `js/sm-inbox.js:23-31` có loai/kenh/endpoint | Không dòng nào nói **điều kiện cấp quyền + chờ bao lâu + ai bấm**: nối Shopee thật cần ủy quyền shop (không phải 1 nút), Zalo OA cần giấy ĐKKD duyệt 2–3 ngày [Q-004], MISA phải gặp NVKD [Q-002] — mockup hiện «Nối» tức thì cho tất cả là nói quá | Mỗi dòng connector thêm 1 dòng phụ «Điều kiện nối: … · Chờ: … · Người bấm: …» — dữ liệu lấy thẳng bảng 12 khối của T2 (`out/T2-CONNECTOR-SPEC.md`) để không đặc tả 2 nơi: sepay «tự làm, ~vài phút» [Q-002], shopee «cần ủy quyền shop, chờ xét duyệt app» [Q-002], zalo «cần GP ĐKKD, BQT duyệt 2–3 ngày làm việc» [Q-004], cqt «15 phút–1 ngày làm việc» [Q-002], misa «qua nhân viên kinh doanh» [Q-002]. Nút «Nối» với dịch vụ có lead-time dài đổi thành «Bắt đầu hồ sơ — đang chờ duyệt X ngày» với đồng hồ đếm. **Luồng tự đăng ký/đăng ký hộ lần đầu: giao T1**; mục này chỉ phần hiển thị trạng thái trên màn có sẵn | PHẢI | S | Màn ketnoi: 100% connector đang nối hiện đủ 3 thông tin điều kiện/chờ/ai-bấm (nhãn nguồn từng số); connector có lead-time hiển thị trạng thái «đang chờ duyệt» + số ngày đã chờ tính từ `tuNgay`; không connector nào gợi ý «OPC bấm thay» (N-06) |
| 7 | **Hỏi–đáp có SLA + «Tính từ đâu» — còn thiếu gì so với «hỏi gì là có»** [R-A3-03 🟡, R-A3-04] | `js/sm-ai.js:1-11` kiến trúc 3 lớp; Lớp A `js/sm-ai.js:114+` mọi câu kèm `tinhTu` (129/145/160/176/191/210/226/241/256); Lớp B KB 10 bài có phiên bản + phê duyệt + nguồn (`js/sm-ai.js:23-93`); Lớp C `js/sm-ai.js:313-333` SLA 4 mức + `escalate()` tạo phiếu; `js/sm-ai.js:341-368` `ask()` | (a) Câu hỏi về **trạng thái vận hành** («đơn này về lúc mấy, từ kênh nào», «kênh nào đang đứt», «dữ liệu tươi bao nhiêu») chưa có handler — dữ liệu có sẵn trong hopthu/ketnoi nhưng không hỏi được; (b) R-A3-04 đòi biết nói «chưa có số này»: lớp C chỉ nói «chưa có trong bộ nội dung», chưa có nhãn «chưa đo — đang hỏi radar Q-0xx» cho số chưa có bằng chứng; (c) SLA trả lời khách ngoài giờ (afterHoursReply) chưa có cam kết thời gian | (a) Thêm 2-3 A-handler mới: «đơn mới nhất về lúc nào/từ đâu» (đọc events của `js/sm-inbox.js`), «kênh nào đang đứt» (đọc trạng thái dòng 5); (b) quy ước nhãn `CHUA_DO` cho mọi chỗ ghi «chưa đo» (màn độ tươi dòng 2 dùng chung nhãn này) — trợ lý trả lời «chưa đo, radar đang hỏi Q-036» thay vì im lặng hoặc bịa; (c) thêm dòng SLA «tin khách nhắn ngoài giờ: trợ lý soạn trong 1 phút, hộ duyệt và bấm gửi» — người bấm là hộ (N-06) | NÊN | S | Hỏi «kênh nào đang đứt» → trả lời đúng với trạng thái dòng 5 kèm «tính từ đâu»; hỏi «đơn sàn cập nhật mấy phút một lần» → câu trả lời có nhãn «chưa đo — Q-036» (không bịa số); afterHoursReply hiển thị cam kết thời gian |
| 8 | **Thuế sàn đã khấu trừ nộp thay — hiển thị trên đơn sàn** [Q-019: NĐ 117/2025/NĐ-CP Điều 11 khoản 4 — hộ đã được sàn nộp thay thì không phải khai/nộp lại; R-A1-06] | `js/sm-inbox.js:198-201` payload đơn Shopee có `hinhThucThanhToan:'Đã thanh toán trên sàn'`; `js/sm-domain.js:134-140` taxEstimate tính GTGT/TNCN trên toàn bộ doanh thu theo nhóm kênh (không tách phần sàn) | Toàn bộ v3 không mô hình hoá «thuế sàn đã khấu trừ» (grep `khấu trừ\|nộp thay` trên js/ chỉ trúng KB-05 và GTGT khấu trừ chế độ DN — khác ý; Q-019 đã grep 0 kết quả trên v2 docs): đơn sàn về nhưng tờ khai tạm tính có thể tính ĐÙNG phần sàn đã nộp thay → số đưa cho hộ sai nghĩa vụ | (a) Payload đơn sàn thêm `thueSanDaNop` (số giả lập trong seed, không viết cứng lên UI); (b) `taxEstimate` tách cột «doanh thu qua sàn — sàn đã nộp thay, không tính lại» [Q-019: Đ11 k4] + dòng giải thích trên màn Tien; (c) chi tiết đơn hiện «phần thuế sàn đã khấu trừ nộp thay: Xđ (nguồn: payload sàn)». Ranh giới: chỉ HIỆN số từ payload, không tự khấu trừ/khai thay (N-06); số liệu đối chiếu cuối kỳ là việc hộ làm trên cổng | NÊN | M | Đơn sàn mới từ kịch bản sanTMDT hiển thị dòng thuế sàn đã nộp thay; màn thuế tạm tính tách riêng phần này ra tổng số phải tự nộp; câu hỏi trợ lý «thuế sàn đã nộp thay bao nhiêu» trả lời được kèm tinhTu |
| 9 | **Đối chiếu giá mình vs KiotViet bundle 270k** [Q-007: 270k/330k/490k mỗi tháng, +270k/+375k mỗi chi nhánh, +150k mỗi kho; mọi gói kèm HĐĐT + chữ ký số + PM kế toán hộ KD MIỄN PHÍ; Q-007 «thieu»: De-An thiếu bảng đối thủ] | `js/sm-program.js:23-79` GIA ba cột (niêm yết/sỉ/năm 3, cấu phần tuân thủ để 0đ); `js/sm-program.js:103-118` CHONG_LAN «bản đồ chồng lấn»; `I:522` comment tự nhận «Đối thủ MISA/KiotViet sẽ soi chỗ nào hô tự động quá…»; `M:898-927` viewChiphi render giá cho hộ | Không bảng «giá mình vs KiotViet 270k full-bundle» ở bất kỳ đâu (grep KiotViet trên code v3 = 0, chỉ có trong docs/plan-v4) → không trả lời được câu hỏi hội đồng «tại sao không lấy 270k có sẵn mọi thứ» [Q-007 «thieu»] | Thêm khối `DOI_THU` vào `js/sm-program.js` (đúng tầng Chương trình — phần hội đồng chấm, KHÔNG nhét vào mobile): bảng so 3 gói KiotViet [Q-007] + câu trả lời định vị «270k mua phần mềm bán hàng kèm tuân thủ 0đ; bên mình bán việc-được-làm-xong đa kênh + trợ lý 24/7 + người xuống cơ sở; lớp tuân thủ mình cũng để 0đ» (khớp nguyên tắc `js/sm-program.js:29-31`); render ở I: cạnh bảng giá IV.9. Tham chiếu chéo T2 dòng `pos` (đã ghi KiotViet làm connector, `out/T2-CONNECTOR-SPEC.md:189-194`) | NÊN | S | index.html hiện bảng KiotViet đúng 3 số gói + 3 dòng kèm miễn phí [Q-007] + 1 câu định vị; mọi số có nhãn nguồn; sm-program có hàm tính tổng để so sánh với giá mình cùng kỳ (không viết cứng tổng) |
| 10 | **Nhập liệu giọng nói tiếng Việt** [R-A2-05 SHOULD; Q-004: 75% MSME online dùng điện thoại làm thiết bị chính — số 2021] | **Không có gì trong code** (grep `giọng\|thu âm\|SpeechRecognition\|ghi âm` trên *.{html,js} = 0; «voice» chỉ trúng nhầm trong từ «invoice»); mới được NÊU: `I:437` ràng buộc IV.4 liệt kê giọng nói; `docs/SOAT-KICH-BAN-7-VAI.md:29` tình huống rẫy «tay dính đất đỏ — nhập bằng giọng nói hoặc phím to» | Không màn nào có nút nói; tình huống rẫy (thu mua chùm 5 nông dân, mỗi người giá khác) chính là chỗ giọng nói đáng giá nhất mà chưa vào | Mô phỏng **trung thực, không giả vờ có ASR**: (a) nút «Nói thay gõ» ở màn Thu mua — bấm mở sheet kết quả nhận dạng THEO KỊCH BẢN (dữ liệu tự chảy vào, đúng quy tắc cấm gõ tay — làm theo mẫu KICH_BAN `js/sm-inbox.js:183-188`); nhãn đậm «MÔ PHỎNG nhận dạng — bản thật cần engine»; (b) hiển thị bản thoại thô + cấu trúc bóc ra (5 người × giá) để người chấm thấy đường vào → dữ liệu; (c) 1 chạm xác nhận từng người. Ghi chú roadmap trong docs | ĐỂ-SAU | M | Màn Thu mua có nút nói; bấm ra đúng chùm 5 nông dân giá khác nhau theo kịch bản; bản thoại thô hiển thị cạnh cấu trúc; nhãn «mô phỏng» hiện rõ; không có chỗ nào claimed có ASR thật |
| 11 | **Thông báo đẩy hành-động-được + brief sáng** [R-A2-03; Q-005 đối chiếu v2 từng có renderZaloBrief; Q-004: chủ hộ đã ở sẵn trong messaging — 62%, số 2021] | Thẻ nhắc «Việc cần để ý» `M:232-243` với nút «Xem» đi thẳng (`data-di`, dòng 242) — map `DICH` có comment «Mỗi việc phải BẤM ĐI THẲNG được» (`js/sm-domain.js:258-262`, đợt 18/08); badge số cảnh báo trên tab Tien `M:209`; hàng đợi hiển thị «N chờ gửi» `M:225-226` | **v3 không còn brief sáng** (grep `Thư ký\|brief\|báo cáo sáng` trên v3 = 0 — v2 có, Q-005 đối chiếu); không có «gói thông báo» buổi sáng gom: việc cần làm hôm nay + tiền về đêm + tin chưa trả lời + trạng thái kết nối, vào 1 chỗ bấm đi thẳng từng việc | Thẻ «Sáng nay cần gì» đầu màn Bán (từ `deadlines()` + events qua đêm của hopthu + queue): 3-5 mục, MỖI mục có nút đi thẳng dùng sẵn cơ chế `data-di` (`M:242`). Phía Zalo chỉ NÊU như kênh nhân bản cho CHỦ HỘ (người vận hành, tương tác hằng ngày → nằm trong cửa sổ 48h miễn phí [Q-005]); không dùng broadcast (N-09) | NÊN | S | Mở app CD1 buổi sáng thấy thẻ gom ≥3 việc từ dữ liệu thật, mỗi việc 1 chạm tới đúng màn; mọi mục truy được về deadlines/events/queue; không mục nào là chữ chết |
| 12 | **Bằng chứng hành vi vào hồ sơ b2g** [Q-004: 75% điện thoại chính · 62% messaging cho kinh doanh (DAI/Ipsos, số 2021, n=999) · «hộ cơ bản không có kiến thức tài chính, kế toán» (hội nghị Cục Thuế 6/2025) · 3,6 triệu hộ quản lý thuế cuối 2024 · ~37.000 hộ >1 tỷ phải dùng HĐĐT máy tính tiền từ 1/6] — tự đề xuất | `B:289` có nguyên tắc offline «dữ liệu xếp hàng đợi»; b2g nói đúng hướng mobile-first nhưng grep `75%\|62%\|24/7` trên b2g.html = 0 — không số khảo sát nào được trích | Căn cứ «vì sao mobile-first / vì sao kênh chat trước» đang là khẳng định không số; hội đồng chấm tỉnh sẽ hỏi «lấy số đâu» | Thêm 1 khối «Căn cứ hành vi» vào b2g.html: 75% / 62% / «không có kiến thức tài chính» / 37.000 hộ >1 tỷ, MỖI số kèm nhãn nguồn + ghi rõ «số 2021, khảo sát Facebook ủy quyền, n=999» như FINDING đã dán (Q-004) — đúng văn phong «nói trước điểm yếu» của `I:512-518` | NÊN | S | b2g.html hiện ≥3 số hành vi, mỗi số có nhãn [Q-004] + hạn chế phương pháp ghi cạnh nhau; không số nào bị trình bày như đo đạc mới nhất |

### Ghi chú dòng «thieu» của từng finding — đã kiểm lại trên v3

- **Q-005 thieu** (cửa sổ 7 ngày + phí tin chưa vào mockup): xác nhận còn thiếu trên v3 → dòng 4.
- **Q-006 thieu** (bảng độ tươi = 0 kết quả): xác nhận còn thiếu trên v3 (grep lượt này = 0) → dòng 2.
- **Q-007 thieu** (bảng giá vs KiotViet): xác nhận chưa có trong code v3 → dòng 9.
- **Q-019 thieu** (thuế sàn khấu trừ nộp thay chưa mô hình hoá): xác nhận chưa có trong v3 js → dòng 8.
- **Q-002 tác động** (mockup chưa có trạng thái kết nối/lead-time): xác nhận → dòng 6.
- **Q-003 tác động** (chưa có màn «agent đang ngủ/đánh thức»): xác nhận → dòng 3.
- **Q-004 tác động** (chưa phản ánh «không đòi kiến thức tài chính»): v3 đã giải quyết một phần bằng chế độ `Aa` chữ lớn + «tính từ đâu» (`js/sm-ai.js`); phần còn thiếu là đưa số hành vi vào hồ sơ → dòng 12.

## 2. Giao nhau với T1 (chỉ ghi chú, không làm thay)

- Trạm kết nối trong onboarding lần đầu: **giao T1** — T1 đã có «Trạm kết nối» (`out/T1-ONBOARDING-FLOW.md:292` nhắc polling 24h + R-A3-05). Màn độ tươi (dòng 2) và lead-time (dòng 6) là NỘI DUNG bảng; T1 quyết nó hiện ở đâu trong luồng đầu.
- Sự kiện «vượt ngưỡng 1 tỷ lần đầu trong onboarding» — cảnh báo trong luồng đầu: giao T1; đồng hồ 30 ngày (dòng 1) là trạng-thái-trường-tồn sau đó.

## 3. Top-10 xếp hạng — việc đáng làm nhất trước

Xếp theo (a) chạm mấy trục A1/A2/A3 cùng lúc, (b) độ «đắt» khi demo cho hội đồng chấm (số thật + kịch bản tự bấm), (c) effort.

| Hạng | Việc (dòng bảng) | Lý do (2 dòng) |
|---|---|---|
| 1 | Đồng hồ 30 ngày đăng ký HĐĐT (dòng 1) | Đây là hậu quả pháp lý duy nhất có «sự kiện + thời hạn» đo được [Q-001] — câu chuyện trung tâm của đề án; CD1 đã vượt mà app im lặng là lỗi kể-chuyện lớn nhất của v3. Demo CD2 sắp vượt → chuông chạy: kịch bản kịch tính nhất seed có sẵn, effort nhỏ nhất/hiệu ứng lớn nhất. |
| 2 | Chính sách tin Zalo lộ phí trước khi gửi (dòng 4) | Đối thủ nào cũng hô «nhắn Zalo miễn phí» — chỉ bên nào hiện 7 ngày + 55đ/165đ mới chứng minh hiểu vận hành thật [Q-005·N-09]. Sheet nhắc nợ có sẵn (`M:2226-2249`), chỉ thiếu 1 hàm tính + vài dòng hiển thị. |
| 3 | Kịch bản «webhook gửi TRÙNG» + khoe dedup (dòng 3, phần b) | «Bấm 2 lần — tiền chỉ cộng 1 lần, có nhật ký chặn trùng» là 10 giây demo thuyết phục hơn trang tài liệu nào về idempotency [Q-006·Q-003]. Nhẹ: kho có sẵn, chỉ thêm 1 kịch bản KICH_BAN + check `id`. |
| 4 | Màn «Độ tươi dữ liệu» per-connector (dòng 2) | R-A3-02 đang ❌ trên sổ sống — đóng nó bằng màn hình công bố được «giây–phút» cho tiền về [Q-006] + nói thẳng «chưa đo» cho phần còn lại (R-A3-04). Hội đồng hỏi «realtime thật không» → có màn trả lời, phối hợp bảng T2. |
| 5 | Connector chết báo ra tiếng (dòng 5) | R-A3-05 là MUST chưa có gì; chuyện «Shopee đứt 2 ngày mà app im lặng» là nỗi sợ thật của hộ. Kịch bản giả lập «đứt → phát hiện bằng poll đối soát → thẻ + nút Kiểm tra» diễn được trọn vòng [Q-003]. |
| 6 | Thuế sàn đã nộp thay hiện trên đơn + tờ khai (dòng 8) | NĐ 117 còn hiệu lực đã xác minh bằng bản ký gốc [Q-019] — không tận dụng là bỏ bằng chứng mua sẵn. Số có trong payload sàn; chỉ thiếu tách cột — tránh cho hộ chuẩn bị sai số phải tự nộp. |
| 7 | Bảng giá vs KiotViet 270k bundle (dòng 9) | DIGEST 20/08 đã gióng «Cần Quang chốt» về 299k vs 270k [Q-007] — bảng này là đòn đỡ đầu tiên khi hội đồng hỏi «tại sao không lấy KiotViet có sẵn mọi thứ». Đúng tầng Chương trình, không đụng mobile. |
| 8 | Màn «Trợ lý chạy nền — đang ngủ, đánh thức bởi gì» (dòng 3, phần a) | R-A3-06/07 đang ❌; đây là cách duy nhất mô tả kiến trúc durable cho người không kỹ thuật mà không vẽ sơ đồ backend [Q-003]. Ghép với dedup (hạng 3) thành một màn «nói thật về mình». |
| 9 | Điều kiện + lead-time cấp quyền trong màn Kết nối (dòng 6) | Bản đồ lead-time 3 lớp của Q-002 (phút / ngày–tuần / đàm phán) là lợi thế thông tin chưa ai trên thị trường hiển thị. T2 đã đặc tả 12 khối — T4 chỉ việc render, không tốn công điều tra thêm. |
| 10 | Thẻ «Sáng nay cần gì» gom việc + đi thẳng (dòng 11) | R-A2-03 MUST; v3 đã có đủ viên ghép (deadlines + data-di + events) chỉ chưa gom. Khôi phục đúng tinh thần brief sáng của v2 đã từng có (Q-005 đối chiếu) mà không phạm N-09 vì gửi cho chủ hộ hằng ngày trong cửa sổ 48h free [Q-005]. |

(Dòng 10 giọng nói và dòng 12 b2g: giá trị thật nhưng không vào Top-10 — giọng nói là SHOULD và dễ làm hỏng uy tín nếu mô phỏng rời rạc; b2g là việc hồ sơ, không phải demo.)

## 4. Tự soát trước khi nộp

- Mọi `file:dòng` trong bảng là kết quả Grep/Read thật lượt này trên `solomatrix-v3-gialai/` — không dòng nào suy ra từ v2.
- Mọi số khí giới có nhãn [Q-00x]/[R-xx]/[N-xx]; số ngoài bảng cho phép trích từ finding đã Read (Q-001…Q-007, Q-019).
- «THIẾU BẰNG CHỨNG» ghi đúng chỗ: độ tươi đơn sàn/tồn kho/vận đơn/tờ khai (Q-036/Q-037), giá CTS rời, điều kiện KiotViet API (Q-044 của T2).
- Không đề xuất nào phạm: N-01 (không tự làm kế toán — dòng 9 chỉ định vị giá), N-06 (mọi nút cuối do NGƯỜI bấm — dòng 4/6/7/11 ghi rõ), N-07 (không chạm tiền khách), N-09 (không broadcast — dòng 4/11).
- Không trùng vai T1: onboarding chỉ ghi «giao T1» (mục 2); không ký tự Trung/Nhật/Hàn trong file; không sửa file nào khác ngoài file này.
