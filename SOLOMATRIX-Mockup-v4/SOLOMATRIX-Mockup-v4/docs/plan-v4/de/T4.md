# ĐỀ T4 — THỢ MỎ: FINDINGS RADAR → MOCKUP v3 CÒN THIẾU GÌ

VAI: người rà soát đối chiếu. Radar OPC đã kiểm chứng được một mớ sự thật (findings Q-001…Q-007, Q-019;
sổ REQUIREMENTS có cột «v2» nhưng mockup nay đã là v3 — ĐÃ KHÁC). Việc của bạn: đối chiếu TỪNG finding
và TỪNG dòng REQUIREMENTS với CODE THẬT của v3, chỉ ra cái gì v3 đã có (kèm file:dòng), cái gì còn thiếu,
và đề xuất cải tiến CỤ THỂ. Đây là mạch «ngoài onboarding» — onboarding T1 lo, bạn chỉ ghi chú chỗ giao nhau.

SẢN PHẨM: `solomatrix-v3-gialai/docs/plan-v4/out/T4-FINDINGS-VAO-MOCKUP.md`

ĐỌC TRƯỚC: cả 8 findings + REQUIREMENTS.md + DIGEST.md (mục «Sản phẩm hiện có — cần sửa gì») ·
grep/Read có chọn lọc trên mobile.html, index.html, b2g.html, js/*.js (cấm nuốt nguyên mobile.html).

## Viết đúng 2 phần:

### 1. Bảng đối chiếu — mỗi dòng một «điểm bằng chứng chạm sản phẩm»
Quét theo danh sách BẮT BUỘC sau (và tự thêm nếu thấy — nhãn «tự đề xuất»):
- **Ngưỡng 1 tỷ động**: sự kiện «doanh thu lũy kế vượt 1 tỷ → đồng hồ 30 ngày đăng ký HĐĐT» [Q-001].
  v3 đã có ngưỡng trong sm-domain (grep `nguong`, `1 tỷ`, `30 ngày`) — có ĐỒNG HỒ ĐẾM chưa? CD2 sắp vượt
  là chỗ demo đắt nhất: mô tả màn/thẻ nhắc cần thêm.
- **Bảng ngân sách độ tươi per-connector** [Q-006, R-A3-02 ❌]: DIGEST 20/08 đã grep v2 = 0 kết quả —
  kiểm lại trên v3; đề xuất màn mobile + mục hồ sơ index.html (phối hợp bảng của T2).
- **Màn «agent đang ngủ / đánh thức bởi gì»** [Q-003, R-A3-06/07 ❌]: mockup thể hiện được gì về
  webhook/poll đối soát/idempotency mà KHÔNG cần backend — nghĩ cách mô phỏng trung thực
  (ví dụ bảng giả lập bắn 2 lần cùng `id` → app chỉ ghi 1 và KHOE điều đó).
- **Chính sách tin Zalo trong màn Hội thoại + hàng đợi gửi đi** [Q-005, N-09]: cửa sổ 7 ngày ·
  8 tin free/48h · 55đ/165đ mỗi tin — khách im lặng >7 ngày thì nút «nhắc nợ»/«upsell» phải đổi đường
  (ZNS trả phí + template duyệt) và HIỆN chi phí. v3 có màn hoithoai + nút nhắc nợ (grep `nhacNo|nhắc nợ`) —
  đã biết gì về 7 ngày chưa?
- **Connector chết phải báo ra tiếng** [R-A3-05]: v3 có trạng thái kết nối lỗi chưa (grep `ketnoi`, `loi`)?
  Đề xuất thẻ cảnh báo + lời thoại.
- **Trạng thái kết nối + lead-time cấp quyền từng dịch vụ** [Q-002]: màn `ketnoi` hiện có gì, thiếu gì.
- **Hỏi–đáp có SLA + «tính từ đâu»** [R-A3-03 🟡]: sm-ai đã có «Tính từ đâu» + SLA lớp C — còn gì thiếu so
  với yêu cầu «hỏi gì là có» (trục A3)?
- **Đối chiếu giá KiotViet** [Q-007]: cái này thuộc tầng Chương trình (index.html/sm-program «bản đồ chồng lấn»?)
  hay đề án tài chính? Soi sm-program.js rồi đề xuất đúng đích, đừng nhét bừa vào mobile.
- **Nhập liệu giọng nói tiếng Việt** [R-A2-05 SHOULD]: v3 có gì chưa; mô phỏng kiểu gì cho thật
  (không giả vờ có ASR thật).
- **Thông báo đẩy hành-động-được** [R-A2-03]: brief sáng/thẻ nhắc hiện có nút đi thẳng chưa (đã có một phần
  từ đợt 18/08 — kiểm và ghi rõ file:dòng).
- Các mục «thieu» ghi trong phần «Đối chiếu sản phẩm hiện có» của TỪNG finding (Q-005, Q-006, Q-007…) —
  kiểm lại từng cái trên v3.

Khuôn mỗi dòng bảng: `#` · điểm bằng chứng [nhãn] · v3 đang có gì (file:dòng thật — PHẢI grep, không đoán) ·
còn thiếu gì · đề xuất (màn/thẻ/hàm) · mức PHẢI/NÊN/ĐỂ-SAU · effort S/M/L · tiêu chí nghiệm thu.

### 2. Top-10 xếp hạng
10 việc đáng làm nhất (trộn mức độ chạm-3-trục + độ đắt khi demo cho hội đồng chấm), mỗi việc 2 dòng lý do.

Tự soát: mọi «file:dòng» là kết quả grep thật · mọi số có nhãn · không CJK · không đề xuất nào phạm
N-01/N-06/N-07/N-09 · không trùng vai với T1 (onboarding chỉ ghi «giao T1»).
